# Backtrace
具体的实验方式为利用内核在堆栈中保存的信息，每个函数都具有其自己的函数堆栈帧（每个帧的大小为一个内存页），在寄存器s0中存放了当前函数堆栈帧的最高地址fp，称其为帧指针。
### while循环终止条件
PGROUNDUP(fp) - PGROUNDDOWN(fp) != PGSIZE
如果fp为0或者无效，则循环结束

在使用代码随想录得方法完成Alarm实验时，在进程中添加struct proc* p来保存寄存器的值，但是一直报错，直接使用其他方法完成，过一段时间再来思考原因。
